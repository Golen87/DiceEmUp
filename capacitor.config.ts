import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nu.golen.fireanddice',
  appName: 'FireAndDice',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
